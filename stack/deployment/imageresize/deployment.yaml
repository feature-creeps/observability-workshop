---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment
spec:
  selector:
    matchLabels:
      app: imageresize
  template:
    metadata:
      labels:
        app: imageresize
    spec:
      containers:
      - name: imageresize
        image: dima-imageresize
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: json-log
        ports:
        - containerPort: 8080
        resources:
          requests:
            memory: "250Mi"
            cpu: "100m"
          limits:
            memory: "800Mi"
            cpu: "1000m"
